/**
 * has 方法用来拦截hasProperty 操作，
 * 即判断对象是否有某个属性时，这个方法会生效，
 * 典型的操作就是in运算符
 *
 * has 方法可以接受两个参数，分别是目标对象，需查询的属性名
 */

/**
 * constructor 方法用于拦截new命令，
 * constructor方法可以接受两个参数
 *  - target 目标对象
 *  - args 构造函数的参数对象
 *
 * constructor 方法返回的必须是一个对象，否则会报错
 *
 */





